// - `v`,
// - 'r-'
// - vi{ - and toggle `o`
// .eslintrc.js

// `V`,
// - indent
// - j - joining lines

// `<C-v>`
// - 0, $
// - A,I
// - `U`, `u`, `~` - casing
// - `o`, `O`
// - sort u
// ...http://vimdoc.sourceforge.net/htmldoc/visual.html#visual-change

// - `gv`

import {
  groupBy,
  map,
  o,
  path,
  prop,
  all,
  path,
  prop,
  allPa,
  path,
  prop,
  allPaPass,
  compose,
  concat,
  filter,
  anyPass,
  T,
  applySpec,
  values,
  reduced,
  sortBy,
  split,
  test,
  join,
  toPairs,
  unnest,
  reduce,
} from "ramda";
